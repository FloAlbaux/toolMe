# PostgreSQL password (db + api in Docker). Dev default: toolme. Set a strong value in prod.
POSTGRES_PASSWORD=VERY_STRONG_PASSWORD

# API (Docker and local). Required for the api service in docker-compose.
# For production: set ENVIRONMENT=production and a strong SECRET_KEY (e.g. openssl rand -hex 32).
# For local/dev: leave SECRET_KEY empty (app uses a built-in dev default).
SECRET_KEY=
ENVIRONMENT=development
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=60

# Backend when running locally: either set DATABASE_URL or the vars below (defaults shown)
# POSTGRES_USER=toolme
# POSTGRES_PASSWORD=toolme
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=toolme
# Or a single URL:
# DATABASE_URL=postgresql+asyncpg://toolme:YOUR_PASSWORD@localhost:5432/toolme

# CORS (M-1). In production set to your frontend origin(s), e.g. https://app.toolme.example
# CORS_ORIGINS=http://localhost:5173

# Seed (M-3). Set RUN_SEED=false in production. If RUN_SEED=true in prod, set a strong SEED_PASSWORD.
# RUN_SEED=true
# SEED_PASSWORD=

# Optional: set to 1 to log SQL queries
# SQL_ECHO=0

# Email (verification + password reset).
# With docker-compose: Mailpit is used by default (SMTP_HOST=mailpit, port 1025).
#   Open http://localhost:8025 to view all outgoing emails.
#   SMTP_SKIP_STARTTLS=true is set for Mailpit (plain SMTP, no TLS).
# Without Docker or to use a real SMTP server, set:
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USE_SSL=false
# SMTP_SKIP_STARTTLS=false
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=noreply@toolme.example
# FRONTEND_URL=http://localhost:5173
# Optional: in dev without SMTP, append each link to this file (e.g. .toolme-emails.log)
# EMAIL_DEV_FILE=

